API Server Status: READY

Server running on: http://localhost:3001
Database: Connected to PostgreSQL
Prisma: Client generated and ready

Available endpoints:
- GET /stats
- GET /invoice-trends  
- GET /vendors/top10
- GET /category-spend
- GET /cash-outflow
- GET /invoices
- POST /chat-with-data

Ready to accept requests!